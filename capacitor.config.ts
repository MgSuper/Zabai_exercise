import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'zabai_exercise',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
